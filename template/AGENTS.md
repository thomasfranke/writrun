# AGENTS.md — entry point for AI agents

<!-- TODO: one paragraph — what this project is, and the order to read
     its docs in: what an agent always reads, what it reads for the
     chapter it is touching, and the task's own spec and anchors, which
     are the brief a title never is.
     Everything above the WritRun section is this project's own;
     WritRun never touches it. -->

## WritRun

This project tracks its work with WritRun. Before touching `work/`,
`docs/`, or any task, spec, or report, read and follow
[`.writrun/AGENTS.md`](.writrun/AGENTS.md).
